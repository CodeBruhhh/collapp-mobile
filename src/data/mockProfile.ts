import type { StudentProfile } from '@/types/profile';

// PLACEHOLDER: replace with the signed-in student's real profile.
export const PLACEHOLDER_PROFILE: StudentProfile = {
  fullName: 'Student Name',
  email: 'student@example.com',
  academicBackground: '',
  targetMajors: [],
  preferredLocations: [],
};
